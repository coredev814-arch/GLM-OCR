fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Inference stack — install torch separately with the right CUDA build:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.3.0
torchvision>=0.18.0
transformers>=5.3.0
accelerate>=0.30.0
safetensors>=0.4.0
pillow>=10.0.0
pdf2image>=1.17.0

# Layout-aware document parsing (PP-DocLayoutV3 + ResultFormatter)
glmocr>=0.1.5
opencv-python-headless>=4.9.0
# Column-split signal processing
scipy>=1.11.0
